# 1.0.1 验证记录

验证日期：2026-09-08。基线为已发布的 1.0.0，包含本次复习启动修复和“修复知识库”按钮。[公开发行页面](https://github.com/812344707/obsidian-review-center/releases/tag/1.0.1)。

## 自动检查

- 37 个测试文件、255 项测试，覆盖启动链、失效标签页、跳转失败、错误页面评分栏、修复备份顺序、维护锁、缓存强制重建、缺失原文保留及重新连接。
- 原始启动回归场景在旧代码中有 6 项失败，修复后通过。
- ESLint 无错误，保留原有 `elapsed_days` 弃用提示；TypeScript、生产构建和发行检查通过。
- 按锁文件安装依赖，并验证本版 `main.js` 与已经完成下述原生操作验收的修复构建逐字节一致。版本号更新不改变程序逻辑。
- 自动发行流程检查 macOS、Linux，额外覆盖纽约时区的日期边界、离线优化及安装包内容；远端运行状态见 [GitHub Actions](https://github.com/812344707/obsidian-review-center/actions/workflows/ci.yml)。

## Obsidian 实际操作

在 macOS、Obsidian 1.13.7 中执行，使用独立测试知识库：

- 实际点击设置中的“修复知识库”，完成核对 1001 篇笔记，并生成 `pre-repair` 完整备份。
- 修复前后 1001 条记录的笔记项目、卡片项目、墓碑和既有历史一致。
- 修复后点击“开始”能打开笔记；关闭该笔记、返回主页后再次开始成功；主页没有评分栏。
- 日常知识库更新并重新加载修复程序后，设置中按钮可见，开始及关闭标签页后再次开始均成功。未执行日常库修复或评分；所核对的 2158 条现有记录，其笔记项目、卡片项目、墓碑和既有历史无变化。

## 安装包与发布核对

完整安装包包含 `main.js`、`manifest.json`、`styles.css`、`LICENSE`、`THIRD_PARTY_NOTICES.txt`、`priority-queue-2.7.0-source.zip` 共 6 个文件，不包含个人设置、笔记或复习数据。构建工具逐文件检查 ZIP 内容并生成 SHA-256 清单。

发行使用版本号同名 Git 标签，提供独立程序附件、完整 ZIP 和 `SHA256SUMS`。上传后重新下载附件，核对远端标签指向、Release 状态与全部文件哈希。

安装包大小为 402724 字节，SHA-256：

```text
8e2e0603131f7d5fce41fdc699eb3348b8c5cc21ace986221acf83a1f12f7d09
```

[本地检查结果](assets/1.0.1-checks.json)。

## 验证边界

尚未验证本版在真实 iOS、iPadOS、Android、手机软键盘、设备休眠恢复和跨设备并发同步下的行为；Windows、Linux 原生界面及最低 Obsidian 1.13.0 仍需实际设备验证。GitHub 发布不等同于 Obsidian 社区目录上架。
