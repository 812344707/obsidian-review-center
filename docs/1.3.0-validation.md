# 1.3.0 验证记录

验证日期：2026-09-11。以已发布的 1.2.0 为基线，验证自定义习题页标签及旧设置兼容。

## 自动检查

- 43 个测试文件、295 项测试通过；纽约时区自然日边界专项 14 项通过。
- 覆盖自定义普通标签、层级标签、去重、非法存储值回退和旧设置默认空列表。
- 覆盖新建习题页时来源标签与自定义标签合并，以及留空时维持原有行为。
- ESLint、TypeScript、生产构建、优化器和发行文件检查通过；保留原有 `elapsed_days` 弃用提示。

## 原生运行验证

在 macOS 的独立临时 Obsidian 1.13.7 测试库中加载最终发行构建，完成以下核对：

- 插件清单和实际运行实例均报告 `1.3.0`。
- 设置 → 习题页显示“习题页标签”，已保存的 `#习题页/自定义` 和 `#共同标签` 在重载后继续显示。
- 来源笔记带有 `#来源/原标签` 和 `#共同标签`；新建习题页实际写入 `来源/原标签`、`共同标签`、`习题页/自定义`，重复的共同标签只保留一次。
- 新页保留独立 `review_id`、来源链接和空白习题区；Obsidian 原生错误检查未捕获异常。

## 安装包与发布核对

安装包只包含 `main.js`、`manifest.json`、`styles.css`、`LICENSE`、`THIRD_PARTY_NOTICES.txt`、`priority-queue-2.7.0-source.zip`，不包含个人设置、笔记或复习数据。打包脚本逐文件回读 ZIP 并生成 SHA-256 清单。

发布提供完整 ZIP、独立程序附件及 `SHA256SUMS`。上传后重新下载并核对标签、Release 状态、附件清单和文件哈希。

安装包为 406675 字节，SHA-256：

```text
f43f965bf43c177d7e1c8e1c715750bff98553b1923bb1915046f6d720b79766
```

[本地发行检查结果](assets/1.3.0-checks.json)。

## 验证边界

本次原生运行测试使用合成笔记和桌面 Obsidian。真实 iOS、iPadOS、Android、Windows/Linux 界面和跨设备并发同步不在本次验证范围内；临时测试库与用户日常知识库完全分离。
