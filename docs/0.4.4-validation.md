# 渐进式复习 0.4.4 验证记录

## 自动检查

`npm run check` 通过：28 个测试文件、176 项测试，以及 TypeScript 检查和生产构建。

覆盖快捷制卡的插入位置、选文保留、填空编号、属性和代码块保护；笔记与卡片独立的文件夹/标签条件；范围变更、改名、删除、离线修改、同步评分及局部更新；待处理建议和行号；设置保存按钮的成功与失败路径。

## 独立 Obsidian 测试库

在 Obsidian 1.13.7 的独立测试库中使用 1,000 篇合成材料。实际点击“制卡”“问答”“填空”均得到预期原文；阅读模式切换编辑成功，选中文字没有丢失。两次制卡只更新当前来源，整库扫描为 0，原笔记和原卡片排程保持一致。

实际设置不同的笔记与卡片范围后，笔记可纳入而卡片不纳入；保存没有触发整库扫描，也没有改写复习记录与历史。修复保存卡死后，使用相同设置窗口重复保存验证正常。

未完成问答显示“问答尚未写完整”和对应处理办法；点击“查看第 29 行”打开正确来源并定位到该行。桌面与 390 px 窄屏下制卡按钮可见，触控高度为 44 px；手机样式下识别条件没有横向溢出。

## 数据边界

正式 Vault 安装前已备份插件及 963 个复习数据文件；安装后原设置与复习数据逐文件散列不变。实体手机与真实软键盘尚未验收。缓存仍只作为可重建的加速层；当前条目未核实时不使用旧内容直接评分。
