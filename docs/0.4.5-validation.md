# 渐进式复习 0.4.5 验证记录

## 自动检查

2026-09-07 执行 `npm run check`：30 个测试文件、193 项测试，以及 TypeScript 检查和生产构建全部通过。

覆盖标准 Basic/Cloze、简写 `Q:/A:`、独立挖空、旧 `[!review]`、中文问答、多段答案、提示、Extra、代码、属性、注释和 Anki ID。身份与同步测试覆盖隐藏 ID 补写、同 ID 多个 `cN`、新增挖空、改名、移出范围、删除、离线修改、手机评分，以及历史和快照以不同顺序到达。

缓存测试覆盖全文 SHA-256、知识库隔离、文本/解析版本/识别设置失效、损坏回退和 IndexedDB 不可用。启动性能测试确认开始不触发全库扫描，评分前仍读取当前来源和最新正式历史。

## 隔离 Obsidian 测试库

实现完成时在 Obsidian 1.13.7 独立 1,000 篇材料库验证：混排笔记识别数量正确；未将问答内部挖空、代码、属性或注释重复制卡；补写隐藏 ID 后重复整理、清空缓存、修改一篇和文件改名均保持原卡片 ID、排程与队列。

实际点击“制卡”“问答”“填空”得到预期正文。挖空以横线显示，揭示后答案回到原句位置，Extra 位于补充区。主页顺序为“整理数据、开始、统计、设置”，已解决同步冲突不会继续显示为待处理。

桌面与 390 × 844 窄屏均检查主页、标准卡选择、挖空揭示和评分按钮，没有横向溢出。实体手机、真实手机软键盘和第三方同步到达延迟尚未验收。

详细证据与规模数据见 [正文制卡、稳定编号与性能验证](body-cards-stable-ids-validation-2026-09-05.md)。
