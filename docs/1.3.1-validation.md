# 1.3.1 验证记录

验证日期：2026-09-20。以已发布的 1.3.0 为基线，验证数据目录及其上级文件夹移动后的路径跟随与持久化。

## 自动检查

- 43 个测试文件、296 项测试通过；纽约时区下同样为 43 个文件、296 项通过。
- 回归测试覆盖直接移动数据目录，以及移动包含数据目录的上级文件夹。
- 核对新路径立即写入当前设置，并以设置格式版本 4 持久化。
- 核对数据目录移动不会进入普通复习素材改名逻辑。
- ESLint、TypeScript、生产构建、优化器和发行文件检查通过；仅有既存的 `elapsed_days` 弃用警告。

## 部署与运行验证

最终发行构建已部署到 macOS 实际 Obsidian 1.13.7 的“源代码”知识库：

- 覆盖发行包的 6 个程序文件，部署前备份 1.3.0 程序文件和 `data.json`。
- 部署前后 `data.json` 的 SHA-256 一致，个人设置未被覆盖。
- 发行目录与实际插件目录的 6 个文件逐项 SHA-256 一致。
- 插件重载后，清单、启用列表和运行实例均报告 `1.3.1`；清空旧错误后再次重载，Obsidian 未捕获新错误。

为避免改动真实复习数据，本轮没有在生产知识库中再次移动数据目录；真实移动与重启的行为由针对路径事件和持久化写入的回归测试覆盖。

## 安装包与发布核对

安装包只包含 `main.js`、`manifest.json`、`styles.css`、`LICENSE`、`THIRD_PARTY_NOTICES.txt`、`priority-queue-2.7.0-source.zip`，不包含个人设置、笔记或复习数据。打包脚本逐文件回读 ZIP 并生成 SHA-256 清单。

发布提供完整 ZIP、独立程序附件及 `SHA256SUMS`。上传后重新下载并核对标签、Release 状态、附件清单和文件哈希。

安装包为 406897 字节，SHA-256：

```text
8de03a6d0432e3d260f3043348d6377888d2fe232e9842e2257b0884729eacbd
```

[本地发行检查结果](assets/1.3.1-checks.json)。

## 验证边界

自动测试使用合成目录事件；真实 iOS、iPadOS、Android、Windows/Linux 与跨设备并发同步不在本次验证范围内。本轮没有删除真实知识库中的任何旧数据目录。
