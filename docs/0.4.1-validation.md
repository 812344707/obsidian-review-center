# 0.4.1 实用统计验证记录

日期：2026-09-03。环境：macOS、真实 Obsidian 1.13.7、独立临时测试知识库。未安装或修改正式知识库。

## 自动验证

`npm run check`：21 个测试文件、115 项测试通过，TypeScript 检查和生产构建通过。其中新增 15 项统计测试，覆盖：

- 新学／复习按本地日去重，重复练习保留评分次数和用时，笔记／卡片分别统计。
- 同步重复日志、跨日撤销、撤销缺少组／标签字段、旧记录缺少时长或分类信息。
- 评分时的组与标签范围、子标签边界、旧数据归属回退、已删除来源历史保留。
- 延迟首次回忆的 24 小时边界、困难计为记得、同日重复排除、无样本／未知前态。
- 通过修订快照还原旧排程，不跨重置或不明确的历史分支拼接数据。
- 7／30／365 天窗口、无活动日、未来／无效日期日志和夏令时 23 小时日期切换。
- 完整到期量不受每日额度限制，逾期／今日／未来分桶，搁置与失效搁置、状态过滤、稳定标识去重。
- 统计过程不修改记录、历史或设置。既有备份与评分测试同时检查评分前排程和用时字段的保留。

## 真实 Obsidian 检查

1. 测试库从 0.4.0 加载 0.4.1，15 份来源记录除扫描 `updatedAt` 外逐项相等，12 个历史文件逐字节相同，共 75 条历史未改写。数据比对见 `qa/data-comparison.json`。
2. 使用仅存在于内存的 48 份示例来源与 528 条笔记／卡片历史检查图表。今日卡片显示新学 6 张、复习 40 张、记录用时 22.8 分钟；逾期 5 张、今日剩余 4 张；近 30 天保留率 277／345，即 80.3%。图中数字均为测试资料。
3. 桌面和手机分别通过 12 项界面断言：未来 30 天有 30 个可点击柱形，切换时间后键盘焦点保留，点击柱形显示日期明细；数量／用时切换有效；缺少计时有说明；组与子标签去重；笔记与卡片分开；统计切换不改变主页模式或数据；月度样本和比例一致。
4. 树完全展开后滚动至 127 像素，进入统计再返回，选中范围、模式和滚动位置一致。
5. 保存“未来 30 天／近 7 天／记录用时”后重新加载插件，三项选择一致；原有 19 项未完成队列、当前索引、答案显示和历史一致。继续能显示原卡片，退出后统计正常。无示例资料写入复习数据库。
6. 最终 Obsidian 错误记录为空。手机模拟切换会重新载入并暂时提供延迟加载的视图；测试等待真实视图加载后继续，不将加载中的状态当作插件故障。

## 界面检查

- 1180×884 桌面、390×844 手机模拟，均检查明暗主题。
- 手机内容宽度与视口均为 390，没有页面横向溢出；未来 7 天图宽 344，30 天图在自身区域内水平滚动。
- 模式、范围、时间切换及柱形按钮触控尺寸至少 44 像素。柱形键盘聚焦和点击均显示可读明细。
- 近 30 天趋势默认滚动至最近日期；返回栏固定，页面内容独立滚动。
- 手机滚动到底时热力图图例底边约 709 像素，位于浮动导航之上，末尾内容可见。
- 无记录时不会显示虚假的 0% 保留率，完全没有计时的日期显示“—”，部分缺失有数量说明。

附图与入口见同目录 `界面对照.html`。图片为实际插件界面，不是设计稿。

## 测试边界

未验证实体 iPhone／Android、其他桌面操作系统、第三方主题、多设备并发写入，以及百万级历史的交互性能。历史时长和旧排程缺失不会自动补齐；已在界面与升级说明中明确其统计范围。本次没有调整任何复习参数或自动重新排程。
