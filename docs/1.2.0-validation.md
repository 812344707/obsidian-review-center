# 1.2.0 验证记录

验证日期：2026-09-11。以已发布的 1.1.0 为基线，验证增量整理和习题页组合时间模板。

## 自动检查

- 43 个测试文件、295 项测试通过。
- 覆盖全库元数据核对、未纳入正文零读取、未变化正文跨扫描和跨重启零读取，以及文件状态或索引正文哈希变化后的重新读取。
- 覆盖材料移出范围时不读取正文且保留进度，重新纳入后再迁移旧格式。
- 覆盖 `{{yyyy-HHmm-ss}}`、拆分时间变量、原有变量、非法变量和不可移植文件名。
- ESLint、TypeScript、生产构建和发行文件检查通过；保留原有 `elapsed_days` 弃用提示。

## 原生运行验证

在 macOS 的独立临时 Obsidian 1.13.7 测试库中加载候选版 `1.2.0`，完成以下核对：

- 使用 `{{title}}-{{yyyy-HHmm-ss}}` 新建习题页，实际生成 `1.2.0-增量验收-纳入-2026-1042-38.md`，并写入独立 `review_id`、原文标签及来源链接。
- 首次整理为当前纳入的测试笔记和习题页补充正文哈希、修改时间、大小和解析规则签名。
- 对未变化材料再次整理时，拦截到的 Vault 正文读取列表为空；对应进度文件 SHA-256 前后相同。
- 同时修改一篇纳入笔记和一篇未纳入笔记后，整理只读取纳入笔记；未纳入笔记没有生成复习记录。
- Obsidian 原生错误检查未捕获异常。

## 安装包与发布核对

安装包只包含 `main.js`、`manifest.json`、`styles.css`、`LICENSE`、`THIRD_PARTY_NOTICES.txt`、`priority-queue-2.7.0-source.zip`，不包含个人设置、笔记或复习数据。打包脚本逐文件回读 ZIP 并生成 SHA-256 清单。

发布提供完整 ZIP、独立程序附件及 `SHA256SUMS`。上传后重新下载并核对标签、Release 状态、附件清单和文件哈希。

安装包为 406502 字节，SHA-256：

```text
869c3593d07f98a1226ce154f6852ee966698c8d2bcdf0206d25d78f306ffe7f
```

[本地发行检查结果](assets/1.2.0-checks.json)。

## 验证边界

本次原生运行测试使用合成笔记和桌面 Obsidian，重点验证数据访问边界和文件结果，没有重新执行完整视觉回归。真实 iOS、iPadOS、Android、Windows/Linux 界面和跨设备并发同步不在本次验证范围内；临时测试库与用户日常知识库完全分离。
