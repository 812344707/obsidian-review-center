# 开发与验证

先调研成熟插件、Obsidian 原生 API 和可复用实现，再决定功能范围。首个正式版优先保证已有流程、身份和进度稳定；新增功能需有明确需求。

## 本地检查

需要 Node.js 22 或更新版本，以及 `zip`、`unzip`（macOS／Linux 打包环境）。

```bash
npm ci
npm run check
npm run check:optimizer
npm run package
```

`check` 依次运行 Obsidian 官方 ESLint、单元测试、TypeScript、生产构建和版本／依赖／文档检查。`package` 生成安装 ZIP 并逐文件验证、检查 SHA-256。打包目录会重建，只允许程序文件、项目许可和第三方资料，不包含个人数据。正式打包前应先完成 `check`。

官方 ESLint 插件的 Obsidian 依赖覆盖为本项目固定的 SDK 版本，避免检查工具反向降级开发 SDK。`ts-fsrs 5.4.1` 的 `elapsed_days` 已被标为未来弃用，当前仍按既有排程格式保存；此兼容提示保留，不关闭弃用检查。路径校验中有意使用的控制字符正则有两处局部说明。

普通构建使用仓库内固定的 WASM，不需要 Rust。重编译方法见 [优化器说明](optimizer/README.md)。浏览器 Worker 只使用 Web API；Node 桥接代码仅在检查脚本中使用。

## 修改约束

- 保留 `review_id`、卡片稳定 ID、排程、历史和旧版本备份兼容。
- 读取不完整数据时停止写入并显示原因；不要把未知数据当作新内容。
- 范围变更与批量修改原文是不同操作，原文修改须保留现有预览、备份和写后核对。
- 常用设置保持紧凑；桌面标签与控件同行，窄屏自然换行。
- 数据和排程修改要有能重现问题的回归测试；纯文案或样式修改无需堆叠实现镜像测试。

## 实际应用验收

使用独立临时知识库。至少检查首次使用、从旧版升级、原文评分与移动、知识点揭示／评分／撤销、设置保存／重载、备份恢复，以及插件关闭后的清理。修改设置框架时还需核对原生设置搜索和弹出窗口。

分别记录桌面、窄屏模拟、真实手机和实际同步结果。不能用单元测试或 390px 桌面模拟替代真机验收。

CI 只检查和生成候选附件，不会创建 GitHub Release。正式发行步骤与尚缺验收见 [1.0.0 准备记录](docs/release-readiness-1.0.0.md)。

问题反馈使用 [GitHub Issues](https://github.com/812344707/obsidian-review-center/issues)，附插件版本、Obsidian 版本、设备、复现步骤和去除个人内容的最小示例。
